find / -name mail
cat /rofs/var/mail/level05

	*/2 * * * * su -c "sh /usr/sbin/openarenaserver" - flag05
ª

This openarenaserver is a script, extracting it with scp gives us this:

#!/bin/sh

for i in /opt/openarenaserver/* ; do
	(ulimit -t 5; bash -x "$i")
	rm -f "$i"
done

This seems to be a loop which executes at most 5 commands from /opt/openarenaserver

